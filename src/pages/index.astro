---
const EMAIL = "isidoro.pannitti@gmail.com";
const INSTAGRAM_HANDLE = "@_isoradio";
const INSTAGRAM_URL = "https://www.instagram.com/_isoradio/";
const IMAGE_URL = "/images/24_2.jpeg";
---

<html lang="it">
	<head>
		<meta charset="utf-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1" />
		<title>isidoro pannitti architetto</title>
		<style>
			:root {
				color-scheme: light;
			}

			* {
				box-sizing: border-box;
			}

			html,
			body {
				margin: 0;
				padding: 0;
				background: #fff;
				color: #000;
				font-family: "Inter", -apple-system, BlinkMacSystemFont, "Helvetica Neue", Helvetica, Arial,
					sans-serif;
				font-size: clamp(0.98rem, 1.05vw, 1rem);
				line-height: 1.45;
			}

			main {
				min-height: 100vh;
				padding: clamp(1.25rem, 3vw, 2.5rem);
				display: grid;
				align-content: start;
				justify-items: start;
				gap: 0.25rem;
				text-transform: lowercase;
			}

			p {
				margin: 0;
			}

			a {
				color: inherit;
				text-decoration: none;
				transition: opacity 140ms ease;
			}

			a:hover,
			a:focus-visible {
				opacity: 0.68;
			}

			.preview {
				margin: 0.85rem 0 0;
				display: grid;
				gap: 0.45rem;
			}

			.preview img {
				display: block;
				width: min(100%, 34rem);
				height: auto;
			}
		</style>
	</head>
	<body>
		<main>
			<p>isidoro pannitti architetto</p>
			<p>
				<a href={`mailto:${EMAIL}`}>{EMAIL}</a>
			</p>
			<p>
				<a href={INSTAGRAM_URL} target="_blank" rel="noreferrer noopener">{INSTAGRAM_HANDLE}</a>
			</p>
			<figure class="preview">
				<img src={IMAGE_URL} alt="24.2" loading="eager" />
				<figcaption>24.2</figcaption>
			</figure>
		</main>
	</body>
</html>
